{
  "name": "devign-mcp",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "shared",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w shared && npm run build -w @devign-mcp/server && npm run build -w @devign-mcp/figma-plugin",
    "build:server": "npm run build -w shared && npm run build -w @devign-mcp/server",
    "build:plugin": "npm run build -w @devign-mcp/figma-plugin",
    "dev:server": "npm run build -w shared && npm run dev -w @devign-mcp/server",
    "dev:plugin": "npm run dev -w @devign-mcp/figma-plugin",
    "start": "npm run start -w @devign-mcp/server"
  },
  "dependencies": {
    "typescript": "^5.7.3"
  }
}
